Validators
==========

Validators are the corner stone of SQVID. They basically take a look at a
specific column and check whether the values in it are "valid". If they are
not, the validation fails and the rows that do not pass the validation
requirement are returned.

Available validators
--------------------

.. automodule:: sqvid.validators
   :members:
